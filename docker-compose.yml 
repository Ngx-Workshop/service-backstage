# docker-compose.yml
version: '3.8'

services:
  backstage:
    build: .
    container_name: backstage
    restart: unless-stopped
    ports:
      - "3008:3008"
    env_file:
      - .env
    networks:
      - ngx-net

networks:
  ngx-net:
    external: true